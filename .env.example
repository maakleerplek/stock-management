# =============================================================================
# INVENTREE CORE SETTINGS
# =============================================================================
COMPOSE_PROJECT_NAME=inventree
INVENTREE_TAG=stable
# InvenTree server URL - update this to match your server URL
INVENTREE_SITE_URL="https://inventree.localhost"
#INVENTREE_SITE_URL="https://192.168.1.100:443"  # Or use your local IP
INVENTREE_EXT_VOLUME=./inventree-data
INVENTREE_LOG_LEVEL=WARNING
INVENTREE_PLUGINS_ENABLED=True
INVENTREE_AUTO_UPDATE=True

# =============================================================================
# INVENTREE DATABASE
# =============================================================================
# DO NOT CHANGE THESE SETTINGS (unless you really know what you are doing)
INVENTREE_DB_ENGINE=postgresql
INVENTREE_DB_NAME=inventree
INVENTREE_DB_HOST=inventree-db
INVENTREE_DB_PORT=5432
# Database credentials - CHANGE THESE FOR PRODUCTION!
INVENTREE_DB_USER=pguser
INVENTREE_DB_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# =============================================================================
# INVENTREE CACHE
# =============================================================================
INVENTREE_CACHE_ENABLED=True
INVENTREE_CACHE_HOST=inventree-cache
INVENTREE_CACHE_PORT=6379

# =============================================================================
# INVENTREE ADMIN ACCOUNT
# =============================================================================
# Uncomment and complete these lines to auto-create an admin account
#INVENTREE_ADMIN_USER=admin
#INVENTREE_ADMIN_PASSWORD=change_this_password
#INVENTREE_ADMIN_EMAIL=admin@example.com

# =============================================================================
# INVENTREE SERVER & PROXY SETTINGS
# =============================================================================
INVENTREE_GUNICORN_TIMEOUT=90
INVENTREE_USE_X_FORWARDED_HOST=True
INVENTREE_USE_X_FORWARDED_PORT=True
INVENTREE_USE_X_FORWARDED_PROTO=True
# Allow unauthenticated access to media files (required for image proxy)
INVENTREE_MEDIA_AUTH_ENABLED=False
# Disable SITE_URL checking (for local multi-access development)
INVENTREE_STRICT_URL_CHECK=False

# =============================================================================
# CORS SETTINGS
# =============================================================================
# Enable CORS for all origins (allows frontend apps to access the API)
# Set to False and configure specific origins for production
INVENTREE_CORS_ORIGIN_ALLOW_ALL=True

# =============================================================================
# NETWORK SETTINGS
# =============================================================================
# Caddy proxy port configuration
INVENTREE_HTTP_PORT=80
INVENTREE_HTTPS_PORT=443
